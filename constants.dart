import 'package:flutter/material.dart';

// list of colors that we use in our app
const kBackgroundColor = Color(0xFFF1EFF1);
const kPrimaryColor = Color(0xFF64B5F6);
const kSecondaryColor = Color(0xFFFFA41B);
const kTextColor = Color(0xFF000839);
const kTextLightColor = Color(0xFF747474);
const kBlueColor = Color(0xFF1A237E);

const kDefaultPadding = 20.0;

const kDefaultShadowww = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 30,
  color: Colors.grey, // Black color with 12% opacity
);

const kDefaultShadoww = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 30,
  color: Colors.white, // Black color with 12% opacity
);
// our default Shadow
const kDefaultShadow = BoxShadow(
  offset: Offset(0, 15),
  blurRadius: 27,
  color: Colors.black12, // Black color with 12% opacity
);
